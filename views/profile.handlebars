<div>You have reached the profile page</div>

<h1>Welcome {{user.name}}</h1>

{{#each userPosts as |post| }}
  <div class="card">
    <div class="title">
      <h2> {{post.title}} </h2>
      {{!-- <p> Posted by {{ user.name }} on {{ date }} </p> --}}
    </div>
    <div class="content">
      <p> {{post.content}} </p>
    </div>
  </div>
{{/each}}


    <form class="form new-project-form">
      <div class="form-group">
        <label for="post-title">Title</label>
        <input class="form-input" type="text" id="post-tle" name="post-title" />
      </div>
      <div class="form-group">
        <label for="project-funding">Content</label>
        <input class="form-input" id="content" name="content" />
      </div>
      <button type="submit" class="btn btn-primary">Create</button>
      
    </form>

<script src="./js/profile.js"></script>







{{!-- <div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Post</h3>

    <form class="form new-project-form">
      <div class="form-group">
        <label for="post-title">Title</label>
        <input class="form-input" type="text" id="post-tle" name="post-title" />
      </div>
      <div class="form-group">
        <label for="project-funding">Content</label>
        <input class="form-input" id="content" name="content" />
      </div>
      <button type="submit" class="btn btn-primary">Create</button>
      
    </form>
  </div>

  {{#if posts.length}}
  <div class="col-md-6 project-list">
    <h3>Current Posts:</h3>

    {{#each posts as |post|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/project/{{project.id}}">{{project.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{project.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="./js/profile.js"></script> --}}
